table! {
    skillblocks (id) {
        id -> Int4,
        username -> Text,
        category -> Varchar,
        offline_category -> Bool,
        skill_name -> Text,
        skill_description -> Text,
    }
}
